export type Environment = {
  WELCOME_ENVS: any;
  NODE_ENV: string;
  PORT: number;
  ENABLE_SWAGGER: boolean;
  DB_HOST: string;
  DB_PORT: number;
  DB_USER: string;
  DB_PASSWORD: string;
  DB_NAME: string;
  DB_SOCKET_PATH: string;
  PASSWORD_BD: string;
};
